{
  "PdfGeneratorService": 
  {
    "Db": 
    {
      "BlobConnectionString": "${PdfGeneratorServiceBlobConnStr}",
      "LogsConnString": "${PdfGeneratorServiceLogsConnStr}"
    },
    "PdfBlobContainer": "${PdfGeneratorServiceBlobContainer}"
  },
  "SlackNotifications": 
  {
    "AzureQueue": 
    {
      "ConnectionString": "${SlackConnString}",
      "QueueName": "${SlackQueueName}"
    },
    "ThrottlingLimitSeconds": "${SlackThrottlingLimitSeconds}"
  }
}
